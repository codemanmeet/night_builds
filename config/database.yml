development:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: development_night_builds
  pool: 10
  username: postgres
  password: test
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: test_night_builds
  pool: 10
  username: postgres
  password: test
  host: localhost

production:
  adapter: postgresql
  encoding: utf8
  reconnect: false
  database: production_night_builds
  pool: 10